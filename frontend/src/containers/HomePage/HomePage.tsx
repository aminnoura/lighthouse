import React, { StatelessComponent } from "react";


type Props = Readonly<{}>

const HomePage: StatelessComponent<Props> =
    () => (
        <h1>Home Page</h1>           
    );

export default HomePage;
